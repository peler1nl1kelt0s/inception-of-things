apiVersion: v1
kind: Service
metadata:
  name: wil-playground-service
spec:
  type: NodePort
  selector:
    app: wil-playground
  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888
    # Bu port, k3d cluster oluşturulurken host'a yönlendirildi (8888:30080)
    nodePort: 30080
